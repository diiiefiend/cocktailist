<form>
  <% if(!rating.isNew()){ %>
    <p class="form-control">
      Reviewed <strong><%= rating.escape('time') %></strong>.
      Feel free to update it below!
    </p>
  <% } %>
  <label for="rating">Rating</label>
  <div class="rating">
    <% for(var i = 1; i <= 5; i++){ %>
      <div class="rating-click-box" data-rating="<%= i %>"></div>
    <% } %>
  </div>
  <br class="clearfix">
  <!-- <datalist id="slider_ticks">
    <% for(var i = 1; i <= 10; i++){ %>
      <option><%= i %></option>
    <% } %>
  </datalist>
  <label for="composition">Composition</label>
  <div class="composition">
    <input id="composition_range" class="slider" type="range" min="1" max="10"
      list="slider_ticks" value="<%= rating.escape('scale_composition') %>"
      oninput="composition_output.value = composition_range.value">
    <output id="composition_output" class="range_value" for="composition_range">5</output>
  </div>
  <br class="clearfix">
  <label for="spirits">Spirits</label>
  <div class="spirits">
    <input id="spirits_range" class="slider" type="range" min="1" max="10"
      list="slider_ticks" value="<%= rating.escape('scale_spirited') %>"
      oninput="spirits_output.value = spirits_range.value">
    <output id="spirits_output" class="range_value" for="spirits_range">5</output>
  </div>
  <br class="clearfix"> -->
  <label for="body">Review</label>
  <textarea name="rating[body]" placeholder="comments (optional)"><%= rating.escape('body') %></textarea>
  <ul id="errors"></ul>
  <button class="submitRating block">submit</button>

  <% if(!rating.isNew()){ %>
    <a href="javascript: void(0);" class="subtext form-control" id="delete-rating">Delete Rating</a>
  <% } %>
</form>
